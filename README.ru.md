# Aether — AV1 и VP9 в Premiere Pro

[![core checks](https://github.com/neoHaDe/aether-premiere-av1-vp9-importer/actions/workflows/core-checks.yml/badge.svg)](https://github.com/neoHaDe/aether-premiere-av1-vp9-importer/actions/workflows/core-checks.yml)

Перетащил файл AV1 или VP9 на таймлайн — и он играет. Без конвертации,
без промежуточных форматов, без ожидания.

### → [Скачать последний выпуск](../../releases/latest)

Windows · Premiere Pro, After Effects, Media Encoder · бесплатно, Apache 2.0 ·
[English version](README.md)

---

**Premiere не умеет ни AV1, ни VP9.** Ни в 2019 году, ни в 2026. Файл AV1
отвергается с «unsupported video compression type av01» — контейнер разобран,
поток опознан, а декодера за ним попросту нет. VP9 обычно приходит в WebM,
который Premiere не открывает вовсе.

Aether добавляет недостающие декодеры. С этим сталкиваешься, если пишешь
через OBS — AV1 даёт ту же картинку заметно меньшим потоком — или работаешь
с материалом из интернета, где VP9 выдают по умолчанию.

Готовое решение есть: Autokroma Influx. Оно платное, и его магазин недоступен
из России.

## Что нужно

Короткий ответ: Windows и приложение Adobe. Больше ничего.

| Требование | Ответ |
|---|---|
| **Система** | Windows x64. macOS нет: ядро переносимо, всё остальное — чистый Win32 |
| **Приложения** | Premiere Pro, After Effects, Media Encoder. Один установщик на все три |
| **Проверено на** | Premiere Pro **2019** (13.1.5), **2025** (25.x), **2026** (26.0.0.72), After Effects **25.3.2**, Media Encoder **25.6.4** |
| **Не пробовали** | Premiere Pro 2020 – 2024. Лежат между двумя проверенными точками, см. примечание |
| **Видеокарта** | **не нужна.** По умолчанию распаковывает процессор, и это быстрее — замеры ниже |
| **NVIDIA** | необязательно: `av1_cuvid`, `vp9_cuvid`, проверено на RTX 5080 |
| **AMD** | необязательно: через D3D11VA, проверено на встроенной Radeon (9800X3D) |
| **Intel** | не пробовали — путь тот же, что у AMD, железа не было |
| **Асинхронная выдача** | включена. На видеокарте 5–12%, на процессоре ничего; отключается настройкой |
| **Панель в Premiere** | по желанию: пункт «Окно → Расширения → Aether» |
| **Права** | администратора, только чтобы поставить |

Плагин ставится в папку, общую для приложений Adobe, поэтому попадает во все
разом — отдельной установки в каждое не нужно.

Пропуск между 2019 и 2025 — решение, а не недосмотр. Версия интерфейса импортёра
была 21 в 2019 году и равна 24 сейчас, то есть 2020–2024 лежат между двумя
заведомо рабочими точками. А наборы функций плагин просит той версии, какая
у хоста есть, а не самой свежей — именно это 2019-й и проверил, взяв седьмую
версию набора кэша кадров там, где 2025-й даёт восьмую. По той же причине
Premiere 2026 заработал без единой правки в коде.

## Что открывает

Дальше — что плагин забирает себе и что с этим умеет. **«Да» значит проверено:**
вживую или автоматической проверкой. Где не смотрели — так и написано.

### Кодеки и контейнеры

| Формат | Поддержка |
|---|---|
| AV1 | **да** |
| VP9 | **да** |
| всё остальное | **отдаётся Premiere** — он открывает это сам |
| MP4, MOV, M4V | **да** — с AV1 или VP9 внутри |
| MKV, WebM | **да** — с AV1 или VP9; новые Premiere сами открывают часть MKV с H.264/AAC |
| MKA, MKV, WebM только со звуком | **да** — когда их не берёт штатный импортёр Adobe |
| MP4 и M4A только со звуком | **нет, намеренно** — Premiere открывает их сам |
| OGV, FLV, TS, VOB | **нет** — там живут другие кодеки, а не эти два |

Список расширений намеренно шире списка кодеков: в MP4 может лежать что угодно,
поэтому файл сначала открывается, а потом отдаётся обратно, если внутри не AV1
и не VP9. Забрать чужой файл хуже, чем не забрать свой.

### Картинка

| Свойство | Поддержка |
|---|---|
| 8 бит | **да** |
| 10 бит | **да** — отдаётся шестнадцатью, в диапазоне Adobe, см. ниже |
| 12 бит | **не пробовали** — путь тот же, что у десяти |
| прозрачность, VP9 в WebM | **да** — только процессором, см. ниже |
| прозрачность у AV1 | **нет** — устроена иначе, не делали |
| чересстрочная развёртка | **нет** |

### Цвет

| Свойство | Поддержка |
|---|---|
| матрица BT.601 / 709 / 2020 | **да** — берётся из файла, а не угадывается |
| полный и урезанный размах | **да** — тоже из файла |
| цветовое пространство сообщается хосту | **да** — первичные цвета и кривая переноса кодами ITU |
| HDR: PQ и HLG | **да** — передаются как есть, сводит Premiere |
| лог-кривые (S-Log, V-Log, C-Log) | **ожидается** — тем же путём, живого материала не было |

### Время

| Свойство | Поддержка |
|---|---|
| постоянная частота кадров | **да** |
| плавающая частота кадров | **да** — кадр ищется по времени, см. ниже |
| файлы, начинающиеся не с нуля | **да** — у картинки и звука общий ноль клипа |
| таймкод из файла | **нет** |

### Звук

| Свойство | Поддержка |
|---|---|
| AAC, Opus, FLAC | **да**, проверено |
| остальное, что умеет ffmpeg | **ожидается** — свой список кодеков только у видео |
| несколько дорожек | **да**, не смешиваются |
| моно, стерео | **да** |
| 5.1 и больше | **не пробовали** — число каналов передаётся как есть |

## Установка

1. Скачай `AetherSetup-x.y.z.exe` из [Releases](../../releases).
2. Закрой Premiere Pro, After Effects и Media Encoder.
3. Запусти. Нужны права администратора: плагины Adobe лежат в общей системной папке.

Либо возьми `Aether-x.y.z-portable.zip` оттуда же и скопируй папку `Aether`
в `…\Adobe\Common\Plug-ins\7.0\MediaCore\` руками. Те же файлы, без
установщика и без единой записи в реестре. Указания лежат в `INSTALL.txt`
внутри архива.

Выбирать ничего не нужно. Установщик спрашивает у Adobe, где лежит общая папка
плагинов — приложения сами записывают её адрес в реестр, — и показывает список
найденных приложений, чтобы ещё до установки было видно, кто именно подхватит
плагин. Если не нашлось ничего, это тоже не беда: плагин можно поставить заранее,
он подхватится, когда приложение появится.

Удаление — через «Программы и компоненты» или деинсталлятором в папке плагина.
Если ставил из архива — просто удали папку.

### Почему этой сборке можно верить

Строго говоря — не надо просто верить, и этот раздел не об этом просит. Это
плагин, который лезет в собственную папку Adobe, от незнакомого человека из
интернета, и установщик не подписан сертификатом: Windows скажет «неизвестный
издатель» и будет права.

Что предлагается взамен:

- **Исходники здесь целиком**, включая ту часть, которая распаковывает видео.
  Ничего не подгружается во время работы и никуда не сообщается.
- **У каждого релиза указана сумма SHA256**, и перед выпуском она сверяется
  с тем файлом, который действительно приложен. Если они разошлись — не ставь
  и заведи issue.
- **Проверки идут на виду**, на каждый пуш, на серверах самого GitHub — значок
  вверху страницы ведёт прямо в них. Материал для проверок порождается на месте,
  а не скачивается.
- **ffmpeg — обычная сборка под LGPL**, непропатченная, из
  [BtbN/FFmpeg-Builds](https://github.com/BtbN/FFmpeg-Builds). Правой кнопкой
  по любой из библиотек → «Свойства» → «Подробно», и в версии продукта стоит
  точное имя сборки — на момент написания `n8.1.2-44-g7c533d0f86-20260818`, —
  по нему её и надо искать в релизах того проекта.
- **Портативный архив воспроизводим**: одна и та же сборка даёт побайтово
  одинаковый zip, поэтому сумму можно проверить, а не принять на слово.
- **Можно собрать самому.** `build.bat` требует Visual Studio и Adobe SDK;
  SDK — единственное, чего нет в репозитории, потому что условия Adobe
  запрещают его распространять.

Единственное, что убрало бы предупреждение Windows совсем, — сертификат для
подписи кода: несколько сотен долларов в год. Когда проект будет стоить этого
для достаточного числа людей, сертификат появится.

Приложение, в которое загрузился плагин, названо в первых строках журнала — этого
достаточно, чтобы в отчёте об ошибке была версия:

```
host: import interface version 21 (SDK headers know 24)
host: PPro 13.1.5
```

Для аппаратного декодирования нужна видеокарта, умеющая AV1: NVIDIA RTX 30
и новее, AMD RDNA 2 и новее (включая встроенную графику Ryzen), Intel Arc
и Xe. Обязательным оно ни на одной из них не является — по замерам процессор
всё равно быстрее.

Для разработки, без сборки установщика, есть `install.ps1`: он кладёт содержимое
`build\plugin` в папку плагинов (от администратора, при закрытом Premiere).

## Диагностика

Узнать, установлен ли плагин, раньше можно было единственным способом —
бросить файл на таймлайн и посмотреть, открылся ли. Способ так себе, а для
разбора неполадки и вовсе никакой.

Теперь есть отчёт: система, все установленные версии приложений Adobe,
состояние плагина, какие декодеры у ffmpeg на самом деле есть, прогон по пяти
вшитым клипам — и **проверка того файла, который не открывается**.

Последнее и есть главное. Отчёт «всё в порядке», выданный человеку, у которого
файл не открывается, хуже отсутствия отчёта: он отвечает не на тот вопрос.
Файл кладётся в проверку наравне со вшитыми, и к нему добавляется то, что
внутри: кодек, разрядность, цвет, частота кадров, дорожки.

Перемотка проверяется тремя запросами: кадр 0, середина, снова кадр 0. Первый
кадр отдаётся почти всегда, он ключевой; ломается обычно возврат.

Кнопка «Скопировать отчёт» подменяет домашнюю папку на `%USERPROFILE%`, имя
пользователя на `<user>`, имя машины на `<machine>`. Отчёт уезжает в публичный
issue, и без чистки мы своими руками научили бы людей публиковать лишнее.

Дверей к этому две, и обе нужны:

```
Aether.exe          окно рядом с плагином
Окно → Расширения   панель внутри Premiere
```

Панель удобнее, окно надёжнее: диагностика нужна ровно тогда, когда Premiere
из-за драйвера не запускается, — а панель внутри Premiere в этот момент
бесполезна.

Считает при этом только одна программа. Панель написана на HTML и распаковывать
видео не умеет никак, поэтому зовёт `AetherDiagnose.exe` — тот самый движок,
которым пользуется окно, — и показывает его ответ. Написать проверки второй раз
на JavaScript значило бы завести вторую правду, а две правды рано или поздно
расходятся.

## Что работает

- **AV1 и VP9** на таймлайне, распаковка процессором или видеокартой — что из этого
  быстрее, ответ неочевидный, см. замеры ниже
- **Дорожки звука не смешиваются.** В записи может быть несколько звуковых потоков
  сразу, и они приезжают отдельными дорожками, а не сводятся в одну
- Перемотка, поиск и экспорт
- **Файлы вообще без видео** — если контейнер из тех, что Premiere не открывает:
  Matroska или WebM с одним звуком. Решает то же правило, что и везде: MP4 или M4A
  со звуком мы отвергаем, их Premiere открывает сам

Файлы с любым другим кодеком сразу уходят штатному импортёру Premiere, так что
с уже имеющимся материалом ничего не меняется.

Оговорка про MKV и WebM: в Premiere 25.2 появился нативный импорт части MKV с
H.264/AAC. Aether его не отбирает: плагин сам разбирает AV1/VP9, а MKV с H.264
отвергает. Новый Premiere после этого откроет файл штатным импортёром, старый
может по-прежнему не суметь. Поведение WebM и Matroska только со звуком так же
зависит от возможностей хоста после отказа Aether.

### After Effects тоже открывает

Плагины Adobe такого рода лежат в папке, общей для всего пакета, поэтому установщик
подставляет импортёр и в After Effects. Это было догадкой, пока не проверили; теперь
это видно в журнале:

```
host: FXTC 25.3.2
imOpenFile8: accepted, AV1 2560x1440, decoder libdav1d
audio: track 0 ... track 3
frame 0 delivered (2560x1440, stride 10240)
```

`FXTC` — это и есть After Effects. Он открыл ту же запись OBS, развёл все четыре
дорожки звука и на предпросмотре запросил 93 кадра, примерно 42 в секунду на 1440p.
Темп задаёт сам After Effects, а не декодер.

Media Encoder отвечает так же. Полный экспорт той же записи вытянул **все 1265
кадров без единой ошибки**, темпом 71 кадр в секунду: клип длиной 21 секунда
отрендерился за 17.8 секунды, то есть плагин — не то, чего экспорт ждёт. Экспорт
из Premiere Pro CC 2019 тоже работает.

Рендер из самого After Effects тоже работает. Проверено по одной записи
на приложение.

## Замеры

RTX 5080, процессор на 16 потоков. VP9 в пределах пары процентов от AV1.

### Аппаратное декодирование выигрывает не там, где ожидаешь

На кадр при чтении подряд:

| 2560×1440@60 | Итого | Декодирование | Из видеопамяти | Цвет |
|---|---|---|---|---|
| `av1_cuvid` (видеокарта) | 7.7 мс — 130 fps | 0.13 мс | **3.4 мс** | **4.2 мс** |
| `libdav1d` (процессор) | 1.25 мс — **800 fps** | 0.55 мс | — | 0.7 мс |

| 3840×2160@60 | Итого | Декодирование | Из видеопамяти | Цвет |
|---|---|---|---|---|
| `av1_cuvid` (видеокарта) | 13 мс — 77 fps | 0.14 мс | 3.4 мс | 9.5 мс |
| `libdav1d` (процессор) | 3.4 мс — **291 fps** | 0.63 мс | — | 2.8 мс |

Программный декодер быстрее в разы, и причина структурная: Premiere нужны кадры
в обычной памяти, поэтому за аппаратное декодирование приходится платить
копированием из видеопамяти, которого программный путь не делает вовсе. Само
декодирование ничтожно в обоих случаях, меньше миллисекунды.

Две оговорки. Замер сделан на одной машине с сильным процессором; на слабом dav1d
проиграет. И у теста машина в единоличном распоряжении, а в реальном монтаже
Premiere борется за те же ядра. Оба пути далеко выше нужных для воспроизведения
60 кадров/с, так что для большинства задач разница не видна. Переключись
настройкой ниже и суди по своему материалу.

### Чего стоят 10 бит

Десятибитный файл предлагается хосту сначала как `BGRA_4444_16u`, а вторым —
восьмибитный, чтобы хосту было на что откатиться. Для обычного 8-битного файла не
меняется ничего: ему по-прежнему предлагается один формат, и замеры выше остались
теми же — 205 кадров/с до этой работы, 205 после.

Но широкий вывод не бесплатен, и платит за него не декодирование, а пересчёт цвета:

| 2560×1440@60 | Итого | Цвет |
|---|---|---|
| выход 8 бит | 4.8 мс — 208 fps | 3.9 мс |
| выход 16 бит | 16 мс — 62 fps | 15.2 мс |

Это вплотную к 60 кадрам/с, и на таймлайне 1440p разница уже имеет значение.
Из этих 15.2 мс на приведение диапазона (см. ниже) приходится лишь 0.3 — остальное
берёт сам пересчёт ffmpeg в шестнадцать бит на канал. На 1280×720 то же сравнение
даёт 609 кадров/с против 219.

### Перемотка

| | |
|---|---|
| Шаг назад | 5.6 мс/кадр (71 мс без кэша) |
| Случайный прыжок | 75 мс — один интервал между опорными кадрами, предел кодека |

Звук: 4 стереодорожки AAC 48 кГц, читаются независимо друг от друга.

## Настройки

По умолчанию распаковка идёт процессором. Поменять — программой **Aether —
настройки**, она ставится вместе с плагином и лежит в меню «Пуск».

| | |
|---|---|
| Автоматически | процессором на машинах от 8 потоков, ниже — видеокартой |
| Процессором (dav1d) | на этом железе быстрее, замеры выше |
| Видеокартой (NVIDIA / Intel / AMD) | оставляет процессор монтажу |

Там же переключается асинхронная выдача: строка `async = off` выключает её,
а `AETHER_ASYNC=0` — то же самое переменной среды. По умолчанию включена: на
процессоре она не стоит ничего, на видеокарте даёт 5–12%, замеры ниже.

Выбор пишется в `%LOCALAPPDATA%\Aether\settings.ini` и применяется после
перезапуска Premiere. Переменная среды `AV1IMPORTER_HARDWARE=0` (или `=1`) главнее
файла и его не меняет, так удобно быстро проверить догадку. В любом случае
выбранный декодер попадает в журнал.

Окна настроек внутри Premiere нет намеренно. Настройка нужнее всего тогда, когда
из-за драйвера видеокарты Premiere не запускается, и внутри него она была бы в этот
момент недоступна.

## Сборка

Нужны (не в репозитории):
- `sdk/` — Adobe Premiere Pro C++ SDK (скачивается с developer.adobe.com, требует Adobe ID;
  условия Adobe для разработчиков принимаешь сам)
- `ffmpeg/` — сборка BtbN, вариант win64-lgpl-**shared** (нужны заголовки, .lib и .dll)
- Build Tools 2022 с набором для C++
- Inno Setup 6 (`winget install JRSoftware.InnoSetup`) — только для установщика

```
build.bat                        сам плагин -> build\Release\Aether.prm
build-test.bat                   проверочные программы
release-check.bat                строгая предрелизная проверка без пропуска SDK
installer\build-installer.bat    установщик -> dist\
```

Проверка без Premiere:

```
build\decoder_test.exe <файл.mp4>                     ядро: метаданные, кадр, скорость
build\plugin_test.exe  <Aether.prm>              загрузка плагина и его ответы
build\host_test.exe    <Aether.prm> <файл.mp4>   весь импорт от поддельного хоста
```

`build-test.bat` намеренно пропускает зависящие от SDK программы, когда SDK
отсутствует. Для публичного CI ядра это полезно, но перед релизом недостаточно.
GitHub не может гонять `plugin_test` / `host_test`: SDK Adobe нельзя класть
на чужой раннер. Этот гейт — `release-check.bat` на машине, где SDK уже есть.
Он требует SDK, собирает и раскладывает настоящий плагин рядом
с FFmpeg DLL (суммы сверяются с `tools/ffmpeg-dll.sha256`), прогоняет проверки ядра, проверяет `.prm` и открывает
представительные файлы во всех профилях поддельного хоста. Пропуск любого слоя
считается ошибкой.


## Как это устроено

Записи о внутреннем устройстве переехали в
**[docs/internals.ru.md](docs/internals.ru.md)**: чего не говорит SDK Premiere,
почему ffmpeg подключён отложенно, чем кадр таймлайна отличается от кадра
источника, где прячется прозрачность, как объявляется HDR и всё остальное.

Для пользования плагином это не нужно. Оно там потому, что каждый из тех
разделов — ошибка, на поиск которой ушёл день, записанная так, чтобы никому
больше дня не стоила.

## Лицензии, условия Adobe и патенты на AV1

Коротко: Apache 2.0 покрывает код этого репозитория и ничего больше. Подробности —
в [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md).

**Код проекта** — Apache License 2.0, см. [LICENSE](LICENSE) и [NOTICE](NOTICE).
Лицензия относится только к исходникам здесь. Ни на Adobe SDK, ни на FFmpeg она не
распространяется, и не могла бы: ни то, ни другое нам не принадлежит.

Релизы до 1.1.0 включительно были под MIT. Смена намеренная: Apache 2.0 выдаёт
права на патенты прямо, а не оставляет их додумывать, и отзывает их у того, кто
пойдёт на проект с патентным иском. Для плагина о кодеках это живой вопрос, а не
формальность; см. патентную оговорку в конце. Ничего из разрешённого MIT не
убыло; добавились условия: указать авторство, отметить свои изменения и приложить
[NOTICE](NOTICE) при распространении.

**Adobe SDK не распространяется.** Ни заголовков, ни примеров кода, ни утилит, ни
любых других частей SDK нет ни в репозитории, ни в релизах: это сделано намеренно,
см. `.gitignore`. Для сборки из исходников SDK нужно получить у
[Adobe](https://developer.adobe.com/premiere-pro/) и принять её условия для
разработчиков. Плагин использует только публичный документированный API и не
изменяет программы Adobe. В релизе распространяется скомпилированный плагин — наш
собственный код в машинном виде, а именно это условия Adobe для плагинов и
предполагают.

Adobe, Premiere Pro и Media Encoder — торговые марки Adobe Inc. Проект независимый:
не связан с Adobe, не спонсируется ею и не одобрен ею.

**FFmpeg** используется по лицензии LGPL v3: неизменённая сборка BtbN, связывание
динамическое, текст лицензии кладётся рядом с плагином. Библиотеки можно заменить
своей сборкой тех же версий; именно эту свободу LGPL и требует.

**AV1 и патенты.** AV1 объявлен Alliance for Open Media свободным от отчислений по
AOMedia Patent License 1.0, но она покрывает только патенты участников AOMedia, и
претензии третьих сторон возможны. В 2026 году Dolby подала иск против Snap именно
по AV1, что показывает: «royalty-free» не абсолютная гарантия. Плагин только
декодирует, кодирования в нём нет вовсе; декодирует драйвер видеокарты либо dav1d
(BSD-2-Clause). Для открытого плагина риск заметно ниже, чем для крупного
коммерческого продукта, но не нулевой. **Это не юридическая консультация.** При
коммерческом применении получите свою.
