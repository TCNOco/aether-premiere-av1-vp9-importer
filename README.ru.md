# Импортёр AV1 / VP9 для Adobe Premiere Pro, After Effects и Media Encoder

[![core checks](https://github.com/neoHaDe/premiere-av1-vp9-importer/actions/workflows/core-checks.yml/badge.svg)](https://github.com/neoHaDe/premiere-av1-vp9-importer/actions/workflows/core-checks.yml)

Перетащил файл AV1 или VP9 на таймлайн — и он работает: без конвертации
и промежуточных форматов.

[English version](README.md)

Premiere Pro **не декодирует ни AV1, ни VP9**. С AV1 демультиплексор контейнер
разбирает и опознаёт поток как `av01`, но декодера нет, и импорт падает с «unsupported
video compression type». VP9 обычно приходит в WebM, а его Premiere не открывает вовсе.
Готовое решение (Autokroma Influx) платное и его магазин недоступен из РФ.

## Заработает ли у тебя

**Приложения.** Плагин ставится в папку, общую для приложений Adobe, поэтому его
подхватывают все они разом. Какие из них проверяли на самом деле:

| | |
|---|---|
| Premiere Pro CC 2019 — 13.1.5 | проверено, самая старая проверенная версия |
| Premiere Pro 2020 – 2024 | не пробовали, ожидается, что работает — см. ниже |
| Premiere Pro 2025 — 25.x | проверено, основная версия |
| After Effects 25.3.2 | проверено: импорт, дорожки звука, предпросмотр, рендер |
| Media Encoder 25.6.4 | проверено: экспорт целиком, 1265 кадров, ноль ошибок |
| macOS | нет. Ядро декодирования переносимо, остальное — чистый Win32 |

Пропуск в середине — решение, а не недосмотр. Версия интерфейса импортёра была 21
в 2019 году и равна 24 сейчас, то есть 2020–2024 лежат между двумя заведомо
рабочими точками. А наборы функций плагин просит той версии, какая у хоста есть,
а не самой свежей — именно это и проверил 2019-й, взяв седьмую версию набора кэша
кадров там, где 2025-й даёт восьмую.

**Форматы.**

| | |
|---|---|
| AV1 | да — MP4, MKV, WebM, MOV, M4V |
| VP9 | да — те же контейнеры |
| Многодорожечный звук | да, дорожки не смешиваются |
| Плавающая частота кадров | да — кадр ищется по времени, а не по номеру |
| Файлы, начинающиеся не с нуля | да — у картинки и звука общий ноль клипа |
| 10 бит | да — отдаются шестнадцатью битами, см. ниже |
| HDR | распаковывается, но метаданные переноса пока не передаются |
| Альфа-канал | да, для VP9 в WebM — распаковка процессором, см. ниже |
| MKV, MKA, WebM только со звуком | да — эти контейнеры Premiere не открывает вовсе |
| MP4 и M4A только со звуком | нет, намеренно — их Premiere открывает сам |
| Любой другой кодек | сразу возвращается штатному импортёру Premiere |

**Железо.** Windows x64, больше ничего. Без подходящей видеокарты плагин
распаковывает процессором — на этом железе это и так быстрее.

## Установка

1. Скачай `AV1Importer-Setup-x.y.z.exe` из [Releases](../../releases).
2. Закрой Premiere Pro, After Effects и Media Encoder.
3. Запусти. Нужны права администратора: плагины Adobe лежат в общей системной папке.

Выбирать ничего не нужно. Установщик спрашивает у Adobe, где лежит общая папка
плагинов — приложения сами записывают её адрес в реестр, — и показывает список
найденных приложений, чтобы ещё до установки было видно, кто именно подхватит
плагин. Если не нашлось ничего, это тоже не беда: плагин можно поставить заранее,
он подхватится, когда приложение появится.

Удаление — через «Программы и компоненты» или деинсталлятором в папке плагина.

Приложение, в которое загрузился плагин, названо в первых строках журнала — этого
достаточно, чтобы в отчёте об ошибке была версия:

```
host: import interface version 21 (SDK headers know 24)
host: PPro 13.1.5
```

Для аппаратного декодирования нужна видеокарта с поддержкой AV1 (NVIDIA RTX 30
и новее).

Для разработки, без сборки установщика, есть `install.ps1`: он кладёт содержимое
`build\plugin` в папку плагинов (от администратора, при закрытом Premiere).

## Что работает

- **AV1 и VP9** на таймлайне, распаковка процессором или видеокартой — что из этого
  быстрее, ответ неочевидный, см. замеры ниже
- **Дорожки звука не смешиваются.** OBS пишет микрофон, игру, дискорд и музыку
  отдельными потоками, и они приезжают отдельными дорожками
- Перемотка, поиск и экспорт
- Контейнеры MP4, MKV, WebM, MOV, M4V, MKA
- **Файлы вообще без видео** — если контейнер из тех, что Premiere не открывает:
  Matroska или WebM с одним звуком. Решает то же правило, что и везде: MP4 или M4A
  со звуком мы отвергаем, их Premiere открывает сам

Файлы с любым другим кодеком сразу уходят штатному импортёру Premiere, так что
с уже имеющимся материалом ничего не меняется.

Оговорка про MKV и WebM: Premiere не понимает эти контейнеры вообще, поэтому плагин
разбирает их сам. Это работает, но только для кодеков из списка выше. MKV с H.264
плагин отвергнет, и Premiere его тоже не откроет.

### After Effects тоже открывает

Плагины Adobe такого рода лежат в папке, общей для всего пакета, поэтому установщик
подставляет импортёр и в After Effects. Это было догадкой, пока не проверили; теперь
это видно в журнале:

```
host: FXTC 25.3.2
imOpenFile8: accepted, AV1 2560x1440, decoder libdav1d
audio: track 0 ... track 3
frame 0 delivered (2560x1440, stride 10240)
```

`FXTC` — это и есть After Effects. Он открыл ту же запись OBS, развёл все четыре
дорожки звука и на предпросмотре запросил 93 кадра, примерно 42 в секунду на 1440p.
Темп задаёт сам After Effects, а не декодер.

Media Encoder отвечает так же. Полный экспорт той же записи вытянул **все 1265
кадров без единой ошибки**, темпом 71 кадр в секунду: клип длиной 21 секунда
отрендерился за 17.8 секунды, то есть плагин — не то, чего экспорт ждёт. Экспорт
из Premiere Pro CC 2019 тоже работает.

Рендер из самого After Effects тоже работает. Проверено по одной записи
на приложение.

## Замеры

RTX 5080, процессор на 16 потоков. VP9 в пределах пары процентов от AV1.

### Аппаратное декодирование выигрывает не там, где ожидаешь

На кадр при чтении подряд:

| 2560×1440@60 | Итого | Декодирование | Из видеопамяти | Цвет |
|---|---|---|---|---|
| `av1_cuvid` (видеокарта) | 7.7 мс — 130 fps | 0.13 мс | **3.4 мс** | **4.2 мс** |
| `libdav1d` (процессор) | 1.25 мс — **800 fps** | 0.55 мс | — | 0.7 мс |

| 3840×2160@60 | Итого | Декодирование | Из видеопамяти | Цвет |
|---|---|---|---|---|
| `av1_cuvid` (видеокарта) | 13 мс — 77 fps | 0.14 мс | 3.4 мс | 9.5 мс |
| `libdav1d` (процессор) | 3.4 мс — **291 fps** | 0.63 мс | — | 2.8 мс |

Программный декодер быстрее в разы, и причина структурная: Premiere нужны кадры
в обычной памяти, поэтому за аппаратное декодирование приходится платить
копированием из видеопамяти, которого программный путь не делает вовсе. Само
декодирование ничтожно в обоих случаях, меньше миллисекунды.

Две оговорки. Замер сделан на одной машине с сильным процессором; на слабом dav1d
проиграет. И у теста машина в единоличном распоряжении, а в реальном монтаже
Premiere борется за те же ядра. Оба пути далеко выше нужных для воспроизведения
60 кадров/с, так что для большинства задач разница не видна. Переключись
настройкой ниже и суди по своему материалу.

### Чего стоят 10 бит

Десятибитный файл предлагается хосту сначала как `BGRA_4444_16u`, а вторым —
восьмибитный, чтобы хосту было на что откатиться. Для обычного 8-битного файла не
меняется ничего: ему по-прежнему предлагается один формат, и замеры выше остались
теми же — 205 кадров/с до этой работы, 205 после.

Но широкий вывод не бесплатен, и платит за него не декодирование, а пересчёт цвета:

| 2560×1440@60 | Итого | Цвет |
|---|---|---|
| выход 8 бит | 4.8 мс — 208 fps | 3.9 мс |
| выход 16 бит | 16 мс — 62 fps | 15.2 мс |

Это вплотную к 60 кадрам/с, и на таймлайне 1440p разница уже имеет значение.
Из этих 15.2 мс на приведение диапазона (см. ниже) приходится лишь 0.3 — остальное
берёт сам пересчёт ffmpeg в шестнадцать бит на канал. На 1280×720 то же сравнение
даёт 609 кадров/с против 219.

### Перемотка

| | |
|---|---|
| Шаг назад | 5.6 мс/кадр (71 мс без кэша) |
| Случайный прыжок | 75 мс — один интервал между опорными кадрами, предел кодека |

Звук: 4 стереодорожки AAC 48 кГц, читаются независимо друг от друга.

## Настройки

По умолчанию распаковка идёт процессором. Поменять — программой **AV1 Importer —
настройки**, она ставится вместе с плагином и лежит в меню «Пуск».

| | |
|---|---|
| Автоматически | процессором на машинах от 8 потоков, ниже — видеокартой |
| Процессором (dav1d) | на этом железе быстрее, замеры выше |
| Видеокартой (NVIDIA / Intel / AMD) | оставляет процессор монтажу |

Выбор пишется в `%LOCALAPPDATA%\AV1Importer\settings.ini` и применяется после
перезапуска Premiere. Переменная среды `AV1IMPORTER_HARDWARE=0` (или `=1`) главнее
файла и его не меняет, так удобно быстро проверить догадку. В любом случае
выбранный декодер попадает в журнал.

Окна настроек внутри Premiere нет намеренно. Настройка нужнее всего тогда, когда
из-за драйвера видеокарты Premiere не запускается, и внутри него она была бы в этот
момент недоступна.

## Сборка

Нужны (не в репозитории):
- `sdk/` — Adobe Premiere Pro C++ SDK (скачивается с developer.adobe.com, требует Adobe ID;
  условия Adobe для разработчиков принимаешь сам)
- `ffmpeg/` — сборка BtbN, вариант win64-lgpl-**shared** (нужны заголовки, .lib и .dll)
- Build Tools 2022 с набором для C++
- Inno Setup 6 (`winget install JRSoftware.InnoSetup`) — только для установщика

```
build.bat                        сам плагин -> build\Release\AV1Importer.prm
build-test.bat                   проверочные программы
installer\build-installer.bat    установщик -> dist\
```

Проверка без Premiere:

```
build\decoder_test.exe <файл.mp4>                     ядро: метаданные, кадр, скорость
build\plugin_test.exe  <AV1Importer.prm>              загрузка плагина и его ответы
build\host_test.exe    <AV1Importer.prm> <файл.mp4>   весь импорт от поддельного хоста
```

Интересен третий. Он притворяется Premiere и прогоняет собранный `.prm` через
открытие, сведения о файле, кадры и звук — а потом делает то же самое, притворяясь
хостом постарше: который знает только седьмую версию набора кэша кадров, который
из времён Premiere 13.0, и который набор кэша не выдаёт вовсе. Кадр обязан
получиться одинаковым во всех случаях.

Это закрывает ту поломку, которая у нас реально была. Хост не выдаёт набор версии,
о которой не слышал, поэтому «прошу самую свежую и точка» на любом Premiere
постарше оставляет нулевой указатель — а выглядит это как «такой возможности нет».
Если вернуть ошибку обратно, два профиля из пяти падают, то есть проверка не
декоративная.

Чего она не покажет: шлёт ли старый Premiere другие запросы и в другом порядке,
иначе ли лежали структуры в старых заголовках SDK, как он читает ресурс `IMPT`.
На это ответит только живая установка.

### Проверки на сборочном сервере

Два скрипта превращают проверку ядра в то, что можно запускать без человека:

```
tools\make-test-media.ps1     делает материал силами ffmpeg, ничего не скачивая
tools\run-core-checks.ps1     прогоняет по нему ядро и судит результат
```

Судейство тут важно не меньше распаковки. Пять файлов обязаны открыться и пройти
все проверки, а два — **быть отвергнутыми**: MP4 с одним звуком и файл с H.264,
оба Premiere прекрасно открывает сам. Плагин, который хватает лишнее, хуже того,
который хватает меньше.

Эта пара запускается на каждый пуш, на серверах GitHub с Windows. Сам плагин там
собрать нельзя: ему нужен Adobe SDK, а его нельзя распространять, значит и на
сборочном сервере его быть не может. Проверяется тот слой, который намеренно
оставлен без единой зависимости от Adobe, — это почти весь код и почти весь риск.

## Как это устроено

```
src/AV1Decoder.{h,cpp}   ядро: ffmpeg, ничего не знает про Adobe SDK
src/AV1Importer.{h,cpp}  слой Premiere SDK — переводит запросы Premiere в вызовы ядра
src/AV1Runtime.cpp       подгрузка ffmpeg из папки плагина
src/AV1Log.{h,cpp}       журнал плагина
src/AV1Settings.{h,cpp}  выбор декодера, общий с окном настроек
tools/settings_app.cpp   само окно настроек, AV1ImporterSettings.exe
tools/decoder_test.cpp   проверка ядра без Premiere
tools/plugin_test.cpp    проверка готового .prm без Premiere
tools/host_test.cpp      поддельный хост: весь импорт без Premiere
```

Ядро намеренно отделено от SDK: его можно собрать и проверить консольной программой,
что снимает 90% отладки без запуска Premiere.

### Как не сломать остальные форматы

Плагин объявляет себя обработчиком `.mp4/.mkv/.webm/.mov/.m4v` с приоритетом 100,
выше штатного импортёра Premiere. Но при открытии он **проверяет кодек потока**, и если
внутри не AV1 и не VP9, сразу возвращает `imBadFile`. Premiere в этом случае передаёт файл дальше
по списку приоритетов, то есть своему обычному импортёру. Так поддержка одного кодека
добавляется, ничего не отбирая у остальных.

### Три условия, без которых Premiere игнорирует плагин

Каждое из них проявляется одинаково: файл не открывается, будто плагина нет:

1. **Приоритет 100 и выше.** Ниже — Premiere разбирает mp4 сам и до плагина
   не доходит. Перебить импортёры самой Adobe можно только со ста.
2. **Ответ на `imGetSupports7/8`** значениями `malSupports7/8`. Это рукопожатие
   о версии интерфейса. Без него Premiere читает сведения о файле и молча уходит,
   не спрашивая ни формат пикселей, ни кадры.
3. **`subType` = `imUncompressed`**, а не `av01`. Распаковываем мы, наружу идут
   готовые пиксели; сказать `av01` — значит отправить Premiere искать декодер,
   которого у него нет.

### Почему среда выполнения C++ статическая

Premiere Pro CC 2019 держит рядом с собой собственную `msvcp140.dll` версии
14.00.24210, собранную ещё Visual Studio 2015. Windows ищет библиотеки по имени
среди уже загруженных в процесс, поэтому плагину, собранному новой студией,
достаётся именно эта старая — и он не инициализируется. `LoadLibrary` отвечает
1114, а Premiere ведёт себя ровно так, будто плагина нет: ни сообщения, ни строки
в журнале, и та самая ошибка «unsupported compression type av01», которую и хотели
починить.

Статическая среда выполнения (`/MT`) убирает зависимость, вместо того чтобы за неё
бороться. Здесь это безопасно: через границу плагина не ходит ни один объект среды
выполнения — с Premiere общаемся его же структурами, с ffmpeg через C-интерфейс,
а библиотеки ffmpeg несут свою среду сами.

`plugin_test` читает таблицу импорта из собранного `.prm` и падает, если там снова
появились `MSVCP*` или `VCRUNTIME*`: машина, на которой плагин собирают, —
единственная, где эту беду не увидеть.

### Почему ffmpeg подключён отложенно

Windows ищет зависимые DLL рядом с исполняемым файлом (то есть рядом с Premiere), но не
рядом с загружаемой библиотекой. При обычной линковке Premiere просто молча не загрузил бы
плагин. Поэтому: `AV1Runtime.cpp` при загрузке сам находит ffmpeg в папке плагина, а сами
библиотеки подключены через `/DELAYLOAD`, и к первому настоящему вызову они уже в процессе.

Отсюда два неочевидных требования сборки:

- **межмодульная оптимизация выключена.** С ней линковщик игнорирует `/DELAYLOAD`;
- **библиотеки импорта пересобраны** в `ffmpeg/lib-msvc`, потому что родные из поставки
  ffmpeg сделаны сторонним инструментом и с ними `/DELAYLOAD` тоже не работает (LNK4199):

```
lib /def:ffmpeg\lib\avcodec-62.def /name:avcodec-62.dll /out:ffmpeg\lib-msvc\avcodec.lib /machine:x64
```
(и так же для `avformat-62`, `avutil-60`, `swscale-9`, `swresample-6`)

### Многодорожечный звук

OBS пишет микрофон, игру, дискорд и музыку отдельными потоками. Плагин отдаёт их
порознь через `imIterateStreams`: поток 0 — видео плюс первая дорожка, потоки
1..N — остальные; на последнем возвращается `malNoError` как признак конца списка.

### Прозрачность прячется сразу в двух местах

Файл VP9 с прозрачностью молча приедет полностью непрозрачным, если не сойдутся
два условия, и ни одно из них о себе не сообщает.

**Альфы нет в самом видеопотоке.** Matroska несёт её отдельным довеском к каждому
кадру и помечает дорожку тегом `alpha_mode`. Формат потока при этом по-прежнему
`yuv420p` — то есть спросить у формата, есть ли прозрачность, значит получить
неверный ответ. Единственный честный источник здесь тег.

**Возвращает её только один из двух декодеров VP9.** Родной `vp9` в ffmpeg теряет
альфу молча, `libvpx-vp9` отдаёт `yuva420p`. Аппаратные декодеры теряют её тоже —
у `vp9_cuvid` замерена сплошная непрозрачность. Поэтому файл с прозрачностью
принудительно уходит на процессор, даже если выбрана видеокарта: потерять альфу
без предупреждения хуже, чем распаковывать медленнее.

`decoder_test` проверяет не намерение, а итог: если файл заявляет прозрачность,
в выданных кадрах должно встретиться больше одного значения альфы.

### Шестнадцать бит у Adobe — не те шестнадцать бит

В форматах `_16u` у Premiere белое стоит на **32768**, а не на 65535 — то же
соглашение, что в After Effects и Photoshop, оно записано на 64-й странице
руководства по SDK. ffmpeg отдаёт полный размах 0–65535, поэтому каждую
составляющую приходится приводить. `(v + 1) >> 1` делает это точно по краям:
65535 переходит в 32768, а 0 остаётся нулём.

Ошибка здесь ничего бы не уронила. Она просто сделала бы каждый десятибитный клип
вдвое пересвеченным и обрезанным по светам — из тех ошибок, что живут долго.

`decoder_test` проверяет обе половины на 10-битном файле: что в выводе больше 256
различных значений на канал (иначе это восьмибитные данные в широком буфере) и что
ничто не выходит за 32768.

### Кадр таймлайна — не кадр источника

Premiere просит кадр номер N таймлайна с постоянной частотой. Источник не обязан
идти в ногу: запись с экрана выдаёт то десять кадров в секунду, то шестьдесят, а
в заголовке контейнера при этом стоит одно число. И счётчики эти не связаны: в
нашем проверочном файле 350 кадров источника на десять секунд, которые таймлайн
60 fps делит на 601.

Поэтому нужный кадр не вычисляется, а **ищется по времени**: последний, чья метка
не позже, чем N, делённое на частоту. Понять, что он последний, можно только
увидев следующий — длительность кадра из контейнера для этого не годится,
Matroska проставляет её из заголовка и пишет всем кадрам одно и то же, как бы
далеко они ни стояли друг от друга.

Заглядывать на кадр вперёд не бесплатно, и замер показал, где именно: чтение
подряд и отматывание назад не изменились, а каждая перемотка платила за лишний
кадр — 3% на случайных прыжках, потому что многопоточный декодер отдаёт кадры не
по одному. Выход в том, что большинству файлов разведка не нужна вовсе: если кадр
лёг ровно в запрошенный момент, ничего лучше уже не будет, и он отдаётся сразу.
Постоянная частота не платит ничего, плавающая — один кадр.

Здесь же чинится вторая половина задачи. Не всякий файл начинается с нуля: запись
из транспортного потока или мультиплексирование с `-copyts` оставляют начало где
угодно. Кадры и отсчёты звука теперь считаются от одного общего нуля клипа —
начала контейнера, — поэтому настоящий сдвиг между картинкой и звуком
сохраняется, а не затирается. До этого такой файл разваливался целиком: любой
запрос возвращал самый первый кадр, а звук был тишиной.

И то и другое проверяется замером, а не на глаз. В двух порождаемых файлах
вспышка в кадре и щелчок в звуке поставлены в один и тот же момент; расстояние
между ними на выходе и есть расхождение, и прогон падает, если они разъехались
больше чем на два кадра.

### Кэш кадров

Замеры показали, что болит не чтение вперёд, а отматывание назад: чтобы показать
кадр, межкадровому кодеку надо вернуться к опорному и декодировать оттуда. В записях
OBS опорные кадры раз в секунду, поэтому каждый шаг назад декодировал полсекунды
видео и выбрасывал результат, который тут же требовался снова.

Поэтому кэш наполняется **только при отматывании назад**: при чтении вперёд кадры
и так идут подряд, и кэш лишь тратил бы память. Кадры переносятся из памяти
видеокарты в обычную (полсотни кадров 1440p в видеопамяти не удержать), предел
256 МБ, теснятся самые дальние от текущей позиции.

### Журнал

`%LOCALAPPDATA%\AV1Importer\log.txt`, перезаписывается при каждом запуске Premiere.
Пишет все запросы Premiere по именам, включая отвергнутые плагином. Именно среди
них пряталась причина отказа. Это главный инструмент отладки: внутри Premiere
любая ошибка импортёра выглядит одинаково.

## Лицензии, условия Adobe и патенты на AV1

Коротко: Apache 2.0 покрывает код этого репозитория и ничего больше. Подробности —
в [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md).

**Код проекта** — Apache License 2.0, см. [LICENSE](LICENSE) и [NOTICE](NOTICE).
Лицензия относится только к исходникам здесь. Ни на Adobe SDK, ни на FFmpeg она не
распространяется, и не могла бы: ни то, ни другое нам не принадлежит.

Релизы до 1.1.0 включительно были под MIT. Смена намеренная: Apache 2.0 выдаёт
права на патенты прямо, а не оставляет их додумывать, и отзывает их у того, кто
пойдёт на проект с патентным иском. Для плагина о кодеках это живой вопрос, а не
формальность; см. патентную оговорку в конце. Ничего из разрешённого MIT не
убыло; добавились условия: указать авторство, отметить свои изменения и приложить
[NOTICE](NOTICE) при распространении.

**Adobe SDK не распространяется.** Ни заголовков, ни примеров кода, ни утилит, ни
любых других частей SDK нет ни в репозитории, ни в релизах: это сделано намеренно,
см. `.gitignore`. Для сборки из исходников SDK нужно получить у
[Adobe](https://developer.adobe.com/premiere-pro/) и принять её условия для
разработчиков. Плагин использует только публичный документированный API и не
изменяет программы Adobe. В релизе распространяется скомпилированный плагин — наш
собственный код в машинном виде, а именно это условия Adobe для плагинов и
предполагают.

Adobe, Premiere Pro и Media Encoder — торговые марки Adobe Inc. Проект независимый:
не связан с Adobe, не спонсируется ею и не одобрен ею.

**FFmpeg** используется по лицензии LGPL v3: неизменённая сборка BtbN, связывание
динамическое, текст лицензии кладётся рядом с плагином. Библиотеки можно заменить
своей сборкой тех же версий; именно эту свободу LGPL и требует.

**AV1 и патенты.** AV1 объявлен Alliance for Open Media свободным от отчислений по
AOMedia Patent License 1.0, но она покрывает только патенты участников AOMedia, и
претензии третьих сторон возможны. В 2026 году Dolby подала иск против Snap именно
по AV1, что показывает: «royalty-free» не абсолютная гарантия. Плагин только
декодирует, кодирования в нём нет вовсе; декодирует драйвер видеокарты либо dav1d
(BSD-2-Clause). Для открытого плагина риск заметно ниже, чем для крупного
коммерческого продукта, но не нулевой. **Это не юридическая консультация.** При
коммерческом применении получите свою.
